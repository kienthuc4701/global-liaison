---
import type Job from '@/interfaces/job'
import fetchApi from "@/lib/strapi";
import BlogLayout from "@/layouts/BlogLayout.astro";

  const posts = await fetchApi<Job[]>({
    endpoint:'jobs',
    wrappedByKey: 'data'
  });
---

<BlogLayout pageTitle="Recruitment">
</BlogLayout>

